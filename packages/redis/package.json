{
  "name": "@freelanceos/redis",
  "version": "0.1.0",
  "private": true,
  "description": "Redis connection manager and lifecycle services for FreelanceOS Modular Monorepo",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node -e \"console.log('[@freelanceos/redis] No unit tests: I/O-only infrastructure package — covered by integration tests in @freelanceos/db and apps/web.')\"",
    "clean": "node -e \"const fs = require('fs'); fs.rmSync('dist', { recursive: true, force: true }); fs.rmSync('tsconfig.tsbuildinfo', { force: true });\""
  },
  "dependencies": {
    "@freelanceos/config": "*",
    "@freelanceos/core": "*",
    "ioredis": "5.4.1"
  },
  "devDependencies": {
    "@freelanceos/ts-config": "*"
  }
}
